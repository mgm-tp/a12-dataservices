## Main use cases

### Build project

```shell script
./gradlew assemble -i -s
```

### List gradle tasks

```shell script
./gradlew tasks --all
```

### Start the Data Services server:

```shell
./gradlew dataservices-server-app:bootRun
```